using IDE.Core;
using IDE.Core.Build;
using IDE.Core.Interfaces;

namespace IDE.Documents.Views
{
    //public class Footprint3DPreviewGlobalHelper
    //{
    //    public Footprint3DPreviewGlobalHelper(IDispatcherHelper dispatcher)
    //    {
    //        var meshHelper = ServiceProvider.Resolve<IMeshHelper>();
    //        _meshHelper = meshHelper;
    //        _dispatcher = dispatcher;
    //    }

    //    private readonly IMeshHelper _meshHelper;
    //    private readonly IDispatcherHelper _dispatcher;
    //    private readonly BoardContextBuilder _boardContextBuilder;
    //    public async Task<BoardPreview3DViewModel> GeneratePreview(ILayeredViewModel layeredViewModel)
    //    {
    //        var canvasModel = layeredViewModel.CanvasModel;
    //        var boardContext = _boardContextBuilder.BuildBoardContext(canvasModel);
    //    }
    //}
}
