<UserControl x:Class="IDE.Documents.Views.SimpleTextDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 

             xmlns:ctrls="clr-namespace:IDE.Controls"
             xmlns:local="clr-namespace:IDE.Documents.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300"
            
             >
    <Grid>
        <ctrls:SimpleTextEditor Text="{Binding Text, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                 Foreground="{StaticResource EditorForeground}"
                                Background="{StaticResource EditorBackground}"
                                FontSize="12"
                                >

        </ctrls:SimpleTextEditor>
    </Grid>
</UserControl>
